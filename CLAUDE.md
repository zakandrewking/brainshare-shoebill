# Claude Code — Project Memory

> ## ⚠️ TODOS.md DISCIPLINE — TOP PRIORITY, NEVER VIOLATE
>
> 1. **Capture first.** The MOMENT the user makes ANY request — including small,
>    quick, or mid-task interruptions — your FIRST action is to add it to
>    `TODOS.md`, before doing anything else. If several land at once, add them
>    all.
> 2. **Never clobber.** Re-read `TODOS.md` before editing it. NEVER remove or
>    overwrite an item without moving it to "Recently shipped" — do not let an
>    edit silently drop an entry.
> 3. **Keep it live.** Mark items in progress; move them to shipped the moment
>    they land.
> 4. **It is committed.** `TODOS.md` is tracked in git. Keep it up to date and
>    commit/push changes so alternative clients can pick up and improve the work.
>
> Missing, dropping, or forgetting to log a request is a process failure.

@AGENTS.md
